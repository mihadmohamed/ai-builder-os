# Source Directory

This directory contains the OS Control Panel application code.

Current V1 slice:

- workspace shell
- workspace summary view
- static agent role cards
- workspace data helpers

Planned next:

- new-project creation flow
- project page with structured requirement editing
- PM discovery flow
- lightweight next-action recommendation surface

Keep runtime data out of `src/`.
