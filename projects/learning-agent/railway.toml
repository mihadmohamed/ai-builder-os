[build]
builder = "DOCKERFILE"
dockerfilePath = "projects/learning-agent/Dockerfile"

[deploy]
healthcheckPath = "/_stcore/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
