# Canonical workspace environment for AI Builder OS
#
# This keeps the main Streamlit surfaces and ParentMate API runnable from the
# canonical repo without depending on an archived virtual environment.

-r projects/parentmate/src/requirements.txt
beautifulsoup4==4.14.2
fastapi==0.136.3
playwright==1.54.0
uvicorn==0.48.0
openai-agents==0.18.2
mcp==1.28.1
PyYAML==6.0.3
